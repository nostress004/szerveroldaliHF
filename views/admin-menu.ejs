<% include header %>
    <main>
        <div class="container">
            <table class="highlight" style="margin-top:50px; border-style: groove">

                <tbody>
                    <% pizzas.forEach(function(pizza) { %>
                        <% include adminpizzaitem %>
                            <% }); %>
                </tbody>
            </table>
        </div>
    </main>
    <% include footer %>